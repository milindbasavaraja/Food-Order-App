import React from "react";
import "./css/header.css";

const header = (props) => {
  return (
    <React.Fragment>
      <header></header>
      <div></div>
    </React.Fragment>
  );
};

export default header;
